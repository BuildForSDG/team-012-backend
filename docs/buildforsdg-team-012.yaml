openapi: 3.0.0
info:
  title: BuildForSDG 2020 (Team 012)
  description: 'Documentation for Backend Project'
  contact:
    email: 'developers@email.com'
  license:
    name: MIT License
    url: 'https://opensource.org/licenses/MIT'
  version: 1.0.0
servers:
  - url: http://localhost:{port}
    description: development server
    variables:
      port:
        description: port where server is running on. defaults to 5000
        default: '5000'
paths:
  /:
    get:
      summary: root endpoint for API
      responses:
        200:
          description: success. displays welcome message
        404:
          description: endpoint/resource does not exist